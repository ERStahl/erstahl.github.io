/* E. R. STAHL — public site config.
   These are NOT secrets: an EmailOctopus form id and public store links
   are meant to live in the browser. Fill amazonFollowUrl once the KDP
   author page is live. The EmailOctopus form is wired via its official
   embed script in index.html (Step 1 of the prequel funnel). */

export const config = {
  // Amazon author "Follow" page (US). Fill after the KDP author page is live.
  amazonFollowUrl: "",
};
